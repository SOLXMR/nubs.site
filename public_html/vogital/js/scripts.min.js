// Custom Scripts for Vogital

// Smooth Scrolling Function
function scrollToSection(id) {
  document.getElementById(id).scrollIntoView({
    behavior: 'smooth'
  });
}

// Initialize AOS (already included in index.html)
// AOS.init();

// Minified JS content
// ...
